<h2>Movie Results</h2>
<%= button_to "Return to Discover Page", user_discover_index_path(@user), method: :get %>

<% @movies.each do |movie|%>
  <p><%= link_to "#{movie[:original_title]}", user_movie_path(@user.id, movie[:id])%>
<% end %>